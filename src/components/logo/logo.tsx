import React from "react";
import * as Styled from "./styles";

export const LogoProps = {};

export const Logo = () => {
  return (
    <Styled.Logo>
      <Styled.Svg></Styled.Svg>
    </Styled.Logo>
  );
};
